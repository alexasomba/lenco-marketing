---
title: "/collections — list collections"
description: "List collections and examples"
readmeUrl: "https://lenco-api.readme.io/v2.0/reference/get-collections"
version: "v2.0"
---

# /collections

GET https://api.lenco.co/access/v2/collections

List collections (paginated). Use query parameters like `page`, `from date`, `to date`, and `status` to filter results.

Response includes an array of collection objects and a `meta` object for pagination.
